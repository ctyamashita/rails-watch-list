<div class="title">
  <h1><%= @list.name %></h1>
</div>

<div class="mx-3 row">
  <%= link_to "🎬 Add a movie", new_list_bookmark_path(@list), class: 'btn btn-dark mb-3 w-100' %>
  <div class="cards">
    <% @list.movies.each_with_index do |movie, index| %>
      <div class="card-movie" style="background-image: linear-gradient(rgba(0, 0, 0, 0) 20%, rgba(0, 0, 0, 0.8) 100%), url(<%= movie.poster_url %>)">
        <%= link_to '✖', bookmark_path(@list.bookmark_ids[index]), method: :delete, class: 'delete-btn' %>
        <div class="rating">
          <h3><%= movie.rating %> ★</h3>
        </div>
        <div class="movie-description">
          <h2><%= movie.title %></h2>
          <p><%= movie.overview.slice(0..110) %>...</p>
        </div>
      </div>
    <% end %>
  </div>
  <%= link_to "Back", lists_path, class: 'btn btn-dark mt-3 w-100' %>
</div>
